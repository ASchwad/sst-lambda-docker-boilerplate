[project]
name = "functions"
version = "0.1.0"
description = "Lambda function (container-mode)handlers"
dependencies = ["boto3", "botocore", "pydantic", "openai"]
requires-python = "==3.11.*"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
